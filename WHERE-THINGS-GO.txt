This archive mirrors your project structure. Copy these over your project,
replacing what's there:

  client/src/            -> replaces client/src   (delete yours first)
  client/vite.config.js  -> replaces yours
  client/.env            -> replaces yours

  server/src/            -> replaces server/src   (delete yours first)
  server/.env.example    -> replaces yours
  .gitignore             -> project root

DO NOT copy or delete:
  node_modules/          in either folder
  server/.env            your real secrets — this archive has none
  package.json           in either folder
  client/index.html

ONE NEW DEPENDENCY, if you haven't installed it already:
  cd server && npm install express-rate-limit
